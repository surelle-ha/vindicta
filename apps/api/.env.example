# Set secrets via wrangler secret put (never commit real values):
#   wrangler secret put RESEND_API_KEY
RESEND_API_KEY=re_your_resend_api_key_here

# Configured in wrangler.toml [vars] — override locally in .dev.vars:
DOWNLOAD_BASE_URL=https://vindicta.surelle.xyz
